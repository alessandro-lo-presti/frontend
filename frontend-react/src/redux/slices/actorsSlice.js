const initialState = {
  actors: [],
  favorites: [],
};

// selectors
const actorsSelector = (state) => state.actorsSlice.actors;
const favoritesSelector = (state) => state.favoritesSelector;

// reducers
export const actorsReducer = () => {};
