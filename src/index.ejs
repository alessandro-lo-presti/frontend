<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>WebApp</title>
        <meta name="description" content="" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />

        <% for(var i=0; i < htmlWebpackPlugin.files.css; i++) { %>
        <link href="<%= htmlWebpackPlugin.files.css[i] %>" rel="stylesheet" />
        <% } %> <% for(var i=0; i < htmlWebpackPlugin.files.chunks; i++) { %>
        <script src="<%= htmlWebpackPlugin.files.chunks[i].entry %>" defer></script>
        <% } %>
    </head>
    <body>


        <header class="header">
            <div class="container">
                <div class="logo">
                    Brandname
                </div>
                <nav class="nav" id="nav">
                    <ul class="nav-list">
                        <li class="nav-link" data-section="HOME">
                            Home
                        </li>
                        <li class="nav-link" data-section="RANKING">
                            Classifica
                        </li>                    
                    </ul>
                </nav>
            </div>
        </header>
        
        
        <main class="main">
            <div class="container" id="main-container"></div>
        </main>


    </body>
</html>
